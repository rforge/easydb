- Add tests for edbQuery() and edbNRow with non NULL 'sRow'
- Testthat (run & complete with dates)
- Complete the vignette
  - Retrieve IDs
  - "About dates in SQLite and columns conversion"
  - Push to CRAN
  - ORDER BY 
  - DISTINCT 
- Add a test for odbcDataSources() before connecting 
  to a database (MySQL)
- Implement edbJoin for JOIN queries (unschedule);

